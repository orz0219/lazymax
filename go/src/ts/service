package com.zdd.api.service.{{.ProjectName}};

import com.zdd.vo.ApiProductVOOut;
import com.zdd.vo.ApiResponse;
import com.zdd.vo.PageVOIn;

import java.util.List;

public interface {{.Class}}Service {

    /** * ----------------- 数据灌入 ------------------- */
    // 灌入{{.Des}}
    ApiResponse<Boolean> mount{{.Class}}();

    /** * ----------------- 数据获取 ------------------- */
    // 获取{{.Des}}
    ApiResponse<{{.Out}}> {{.LowClass}}({{.In}} in);
}